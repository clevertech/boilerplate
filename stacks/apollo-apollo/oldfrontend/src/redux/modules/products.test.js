// import reducer, { defaultState } from 'redux/modules/products';
// import deepFreeze from 'deep-freeze';

describe('(Redux) products', () => {
  describe('(Reducer)', () => {
    it('sets up initial state', () => {
      // expect(reducer(undefined, {})).to.eql(defaultState);
    });
  });
});
