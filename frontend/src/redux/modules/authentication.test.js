// import reducer, { defaultState } from 'redux/modules/authentication';
// import deepFreeze from 'deep-freeze';

describe('(Redux) authentication', () => {
  describe('(Reducer)', () => {
    it('sets up initial state', () => {
      // expect(reducer(undefined, {})).to.eql(defaultState);
    });
  });
});
