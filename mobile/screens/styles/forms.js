import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    borderColor: 'gray',
    borderWidth: 1,
    padding: 10,
    margin: 10,
    marginBottom: 40,
    height: 40,
    flexGrow: 1
  }
});
