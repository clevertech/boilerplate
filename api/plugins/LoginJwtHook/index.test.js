describe("LoginJwtHook plugin", () => {
  test.todo("hooks the login payload")
  test.todo("signs the JWT")
  test.todo("saves the JWT to redis")
  test.todo("returns the JWT as an HTTP-only, SameSite, Secure cookie")
})